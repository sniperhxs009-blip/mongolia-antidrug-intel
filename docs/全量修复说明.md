## 本次修复清单
1. 放开GOGO/IKON/NEWS.MN/UB POST蒙古本土媒体
2. 检索周期30天→1年
3. 单站点每日抓取2次→10次
4. 递归深度0→2，抓取新闻详情页
5. 放宽文本过滤规则，媒体页面无需含蒙古文字也入库
6. 缩减黑名单，取消禁毒相关内容误过滤
7. 开启论坛、外媒补充检索
8. 合规永久禁用VPN/代理，屏蔽无法直连gov.mn站点

## 全链路增产修复（2026-07 第二轮）

### 根因（此前仅 1 条）
1. `search_feeds.run` **整段剔除** `site:*.gov.mn` 查询 → 官方快照零产出  
2. 检索任务被切片到极少补盲；论坛几乎只在 full 且仅 6 路  
3. RSS 硬截断 18/30；站内翻页不足；去重过激；UNODC/短讯过严  

### 本轮落地
- 保留 `site:*.gov.mn` 快照检索；gov.mn **直链**仍禁止 HTTP；入库改写为谷歌新闻包装链  
- news/full 均跑论坛（`FORUM_WHEN=1y`）+ 统计/PDF；词库不再过度切片  
- 站内搜 10 页×30 条；RSS 不截断；谷歌最多 8 轮  
- 本土媒体日限额 **50**；UA 池 10；栏目发现蒙语词 + depth 完整生效  
- `content_hash` 含机构+日期；报告单模块 **80** 条  

详见代码文件顶部「本次全链路增产修复」注释。
